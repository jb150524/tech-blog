<div class="post">
    <article class="card">
        <div class="card-header flex-row justify-space-between">
            <h2>{{post.title}}</h2>
        ≈
        Posted by {{post.User.username}} on {{form_date post.createdAt}}
    </article>
</div>