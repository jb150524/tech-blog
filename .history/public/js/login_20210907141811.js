const loginFormHandler = async (event) => {
    event.preventDefault();

    const usernameEl = document.querySelector('#username-input-login').ariaValueMax.trim;
    const passwordEl = document.querySelector('#password-input-login');
}